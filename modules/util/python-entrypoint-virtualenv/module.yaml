schema_version: 1

name: "telicent.container.util.python-entrypoint-virtualenv"
version: "1.0"

description: "Creates the entrypoint script for Python environment activation."

artifacts:
  - name: python-entrypoint-artifact
    path: files/python-entrypoint
    target: entrypoint
    dest: /usr/local/bin/

execute:
- script: perms