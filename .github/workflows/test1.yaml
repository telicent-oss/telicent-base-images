name: Build Images

on:
  push:
    branches:
      - dev

jobs:
  changed-images:
    uses: ./.github/workflows/detect-changed-images.yaml

  build-images:
    needs: changed-images
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: ${{ needs.determine-changes.outputs.images }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build and Deploy Image
        run: |
          echo "Building image for: ${{ needs.changed-images.outputs.images }}"
